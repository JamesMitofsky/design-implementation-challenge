import { Divider as HorizontalRule } from "@mui/material";
const Divider = () => {
  return (
    <HorizontalRule
      light={true}
      sx={{ borderBottomWidth: 0.2, borderColor: "#2d2d2d" }}
    />
  );
};
export default Divider;
